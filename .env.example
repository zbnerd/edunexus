# =============================================================================
# EduNexus Environment Variables Example
# =============================================================================
# Copy this file to .env and fill in the actual values
# DO NOT commit .env file to version control
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration (MySQL)
# -----------------------------------------------------------------------------
# Database username for all microservices
DB_USERNAME=edu

# Database password for all microservices
# Generate a strong password: openssl rand -base64 32
DB_PASSWORD=

# -----------------------------------------------------------------------------
# JWT Configuration
# -----------------------------------------------------------------------------
# JWT secret key for user authentication
# Generate a strong secret: openssl rand -base64 64
JWT_SECRET=

# -----------------------------------------------------------------------------
# Redis Configuration
# -----------------------------------------------------------------------------
# Redis password (if authentication is enabled)
# Generate a strong password: openssl rand -base64 32
REDIS_PASSWORD=

# -----------------------------------------------------------------------------
# Service Discovery (Eureka)
# -----------------------------------------------------------------------------
# Eureka server URL for service registration
EUREKA_DEFAULT_ZONE=http://edu-nexus-discovery:8000/eureka/

# -----------------------------------------------------------------------------
# Kafka Configuration
# -----------------------------------------------------------------------------
# Kafka bootstrap servers for event-driven communication
KAFKA_BOOTSTRAP_SERVERS=kafka1:9092,kafka2:9093,kafka3:9094

# -----------------------------------------------------------------------------
# Distributed Tracing (Zipkin)
# -----------------------------------------------------------------------------
# Zipkin endpoint for distributed tracing
ZIPKIN_ENDPOINT=http://localhost:9411/api/v2/spans

# -----------------------------------------------------------------------------
# Service-Specific Configuration
# -----------------------------------------------------------------------------

# Course Service
COURSE_SERVICE_DB_URL=jdbc:mysql://edu-nexus-mysql:3306/edu_nexus_course
COURSE_SERVICE_PORT=8001

# Enrollment Service
ENROLLMENT_SERVICE_DB_URL=jdbc:mysql://edu-nexus-mysql:3306/edu_nexus_enrollment
ENROLLMENT_SERVICE_PORT=8002
ENROLLMENT_GRPC_PORT=9002

# File Management Service
FILE_SERVICE_DB_URL=jdbc:mysql://edu-nexus-mysql:3306/edu_nexus_files
FILE_SERVICE_PORT=8003

# User Service
USER_SERVICE_DB_URL=jdbc:mysql://edu-nexus-mysql:3306/edu_nexus_user
USER_SERVICE_PORT=8004

# Playback Service
PLAYBACK_SERVICE_DB_URL=jdbc:mysql://edu-nexus-mysql:3306/edu_nexus_playback
PLAYBACK_SERVICE_PORT=8005
PLAYBACK_GRPC_PORT=9005

# GraphQL Gateway
GRAPHQL_PORT=8006

# Discovery Service
DISCOVERY_PORT=8000

# -----------------------------------------------------------------------------
# External API Keys (if needed)
# -----------------------------------------------------------------------------
# Add any external API keys here
# Example:
# STRIPE_API_KEY=
# SENDGRID_API_KEY=
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=

# -----------------------------------------------------------------------------
# Security Notes
# -----------------------------------------------------------------------------
# 1. Never commit .env file to version control
# 2. Use strong, unique passwords for production
# 3. Rotate secrets regularly (at least every 90 days)
# 4. Use different secrets for dev/staging/production
# 5. Store production secrets in a secure vault (e.g., HashiCorp Vault, AWS Secrets Manager)
# 6. Use environment-specific .env files (e.g., .env.dev, .env.prod)
